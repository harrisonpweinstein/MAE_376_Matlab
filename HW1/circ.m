%User defined function to compute the perimeter P and the area A of a
%circle of radius r

function[P,A] = circ(r)
    P = 2*pi*r;     %calculate perimeter of circle with rad r
    A = pi*r^2;     %calculate area of circle with rad r
end

    